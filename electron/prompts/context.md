## Background context

Background context has been provided to help you interpret the transcript
(jargon, domain, project names, etc.). Use it to disambiguate phonetic errors and
choose better terminology. Do NOT include the context itself in the output — it is
for your interpretation only.
