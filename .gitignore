node_modules
dist
release
coverage
.DS_Store
*.log

# whisper.cpp build cache + vendored binary (regenerated by scripts/vendor-whisper.sh)
.whisper-build
resources/whisper

# Secrets / local env — never commit
.env
.env.*
*.pem
*.key
